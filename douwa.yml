pipeline:
  compile:
    image: golang:1.8.5
    commands:
      - go build -o dist/startup

  docker:
    image: douwa/docker
    registry_name: douwa
    repo_name: startup
    dockerfile: Dockerfile
    context: .
    tags: latest

  deploy:
    image: douwa/kube
    cluster_name: kube1
    template: deployment.yaml
